# Seguro Database Analysis

Each subdirectory here implements a performance test for the respective database
technology, initializing a 3 node database cluster and timing how long it takes
to persist a number of mock Urbit events to it. See subdirectories for build and
execution instructions.
